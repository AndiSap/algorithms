apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDirs = ["test"]  // Note @Peter's comment below
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
